name: video_player_example
description: Demonstrates how to use the video_player plugin.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.7.0 <4.0.0'
  flutter: ">=3.29.0"

dependencies:
  flutter:
    sdk: flutter
  video_player:
    path: ../

dev_dependencies:
  analysis_lints: ^1.0.1
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
