name: video_player_example
version: 1.0.0+1
publish_to: "none"
description: "Demonstrates how to use the video_player plugin."

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  video_player:
    path: ../

dev_dependencies:
  analysis_lints: ^1.0.2
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
